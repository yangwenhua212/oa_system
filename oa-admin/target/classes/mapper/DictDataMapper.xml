<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.system.mapper.DictDataMapper">

    <select id="selectDictDataList" resultType="com.oa.system.entity.DictData">
        SELECT * FROM sys_dict_data WHERE del_flag = 0
        <if test="dictType != null and dictType != ''">
            AND dict_type = #{dictType}
        </if>
        ORDER BY sort, id
    </select>

    <select id="selectDictDataByType" resultType="com.oa.system.entity.DictData">
        SELECT * FROM sys_dict_data WHERE dict_type = #{dictType} AND del_flag = 0 ORDER BY sort
    </select>

</mapper>