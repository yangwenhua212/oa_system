<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.system.mapper.ProcessInstanceMapper">

    <select id="selectProcessInstanceList" resultType="com.oa.system.entity.ProcessInstance">
        SELECT * FROM oa_process_instance WHERE del_flag = 0
        <if test="userId != null">
            AND applicant_id = #{userId}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="selectMyProcessList" resultType="com.oa.system.entity.ProcessInstance">
        SELECT * FROM oa_process_instance
        WHERE del_flag = 0 AND applicant_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="selectPendingProcessList" resultType="com.oa.system.entity.ProcessInstance">
        SELECT * FROM oa_process_instance
        WHERE del_flag = 0 AND current_approver_id = #{approverId}
          AND status = 'pending'
        ORDER BY create_time DESC
    </select>

</mapper>
