<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.system.mapper.SysLogMapper">

    <select id="selectDailyOperationStats" resultType="java.util.Map">
        SELECT
            DATE(create_time) AS `date`,
            SUM(CASE WHEN operation LIKE '%登录%' OR operation = 'login' THEN 1 ELSE 0 END) AS loginCount,
            SUM(CASE WHEN operation NOT LIKE '%登录%' AND operation != 'login' THEN 1 ELSE 0 END) AS actionCount
        FROM sys_log
        WHERE create_time >= #{startDate}
          AND create_time &lt; DATE_ADD(#{endDate}, INTERVAL 1 DAY)
        GROUP BY DATE(create_time)
        ORDER BY `date`
    </select>

</mapper>
