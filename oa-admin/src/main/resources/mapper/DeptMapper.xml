<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.system.mapper.DeptMapper">

    <!-- 查询所有部门树 -->
    <select id="selectDeptTree" resultType="com.oa.system.entity.Dept">
        SELECT * FROM sys_dept WHERE del_flag = 0 ORDER BY sort, id
    </select>

    <!-- 查询子部门ID列表 -->
    <select id="selectChildrenDeptIds" resultType="java.lang.Long">
        SELECT id FROM sys_dept WHERE parent_id = #{parentId} AND del_flag = 0
    </select>

    <!-- 查询部门列表 -->
    <select id="selectDeptList" resultType="com.oa.system.entity.Dept">
        SELECT * FROM sys_dept WHERE del_flag = 0
        <if test="parentId != null">
            AND parent_id = #{parentId}
        </if>
        <if test="deptName != null and deptName != ''">
            AND dept_name LIKE CONCAT('%', #{deptName}, '%')
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY sort, id
    </select>

</mapper>
