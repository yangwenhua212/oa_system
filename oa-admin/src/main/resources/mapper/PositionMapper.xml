<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.oa.system.mapper.PositionMapper">

    <select id="selectPositionList" resultType="com.oa.system.entity.Position">
        SELECT * FROM sys_position WHERE del_flag = 0
        <if test="positionName != null and positionName != ''">
            AND position_name LIKE CONCAT('%', #{positionName}, '%')
        </if>
        ORDER BY sort, id
    </select>

</mapper>